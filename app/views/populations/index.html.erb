<h1>Get your population numbers here</h1>
<%= form_for @population, url: populations_by_year_path, html: {method: :get}  do |f| %>
  <div class="field">
    <%= f.label :quantity %>
    <%= f.number_field :year %>
    <%= f.submit "Submit" %>
  </div>
<% end %>